# Kaisa Labs — Environment Configuration
# File: .env (gitignored) | Template: .env.example (committed)
# Last Updated: 2026-05-28
# WARNING: Replace all [REPLACE_ME] with actual values before CI/CD

# CORE APPLICATION
N8N_HOST=[SENSOR]
N8N_PORT=[SENSOR]
N8N_PROTOCOL=[SENSOR]
WEBHOOK_URL=[SENSOR]
TZ=[SENSOR]

# N8N SECURITY
N8N_BASIC_AUTH_ACTIVE=[SENSOR]
N8N_BASIC_AUTH_USER=[SENSOR]
N8N_BASIC_AUTH_PASSWORD=[SENSOR]
N8N_ENCRYPTION_KEY=[SENSOR]
N8N_API_KEY=[SENSOR]

# EXTERNAL INTEGRATIONS — TELEGRAM (used by validate.yml + n8n)
TELEGRAM_BOT_TOKEN=[SENSOR]
TELEGRAM_CHAT_ID=[SENSOR]
TELEGRAM_AGENCY_BOT_TOKEN=[SENSOR]

# EXTERNAL INTEGRATIONS — GOOGLE
GOOGLE_SHEET_ID=[SENSOR]
GOOGLE_SHEET_RANGE=[SENSOR]

# INFRASTRUCTURE
REDIS_PASSWORD=[SENSOR]
OLLAMA_HOST=[SENSOR]
OLLAMA_MODEL=[SENSOR]

# DATABASE CREDENTIALS
POSTGRES_PASSWORD=[SENSOR]
MARIADB_ROOT_PASSWORD=[SENSOR]
MARIADB_PASSWORD=[SENSOR]
MYSQL_ROOT_PASSWORD=[SENSOR]

# GHOST SPECIFIC
GHOST_DB_PASSWORD=[SENSOR]

# DOLIBARR SPECIFIC CREDENTIALS
DOLI_ADMIN_PASSWORD=[SENSOR]
DOLI_ADMIN_LOGIN=[SENSOR]
DOLIBARR_DB_NAME=[SENSOR]
DOLIBARR_DB_USER=[SENSOR]

# CORTEZA SPECIFIC
CORTEZA_DB_USER=[SENSOR]
CORTEZA_DB_NAME=[SENSOR]
DOMAIN=[SENSOR]

# PRODUCTION OVERRIDES (Hetzner)
# Uncomment & update only in .env.production
# CLOUDFLARE_TUNNEL_TOKEN=[CHANGE_ME]
# UFW_ALLOW_CLOUDFLARE_ONLY=true
# DOMAIN_MAIN=automation.privacyfirstautomation.com


# --- MONITORING CONFIG (Uptime Kuma) ---
UPTIME_KUMA_HOST=[SENSOR]
UPTIME_KUMA_PORT=[SENSOR]
